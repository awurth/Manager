{% extends 'app/user_settings/layout.html.twig' %}

{% block page_content %}
  {{ form_start(form, { attr: { class: 'mt-4' } }) }}
    {{ form_rest(form) }}
    <button type="submit" class="btn success mt-4">Enregistrer le mot de passe</button>
  {{ form_end(form) }}
{% endblock %}
