{% extends 'app/layout.html.twig' %}

{% block body %}
<div class="container mx-auto p-4">
  <div class="page-title">
    <h1>Nouveaux identifiants</h1>
  </div>
  {{ form_start(form) }}
    {{ form_rest(form) }}
    <button type="submit" class="btn success mt-4">Créer</button>
  {{ form_end(form) }}
</div>
{% endblock %}
