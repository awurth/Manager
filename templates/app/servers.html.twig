{% extends 'app/layout.html.twig' %}

{% block page_content %}
  <div class="page-title">
    <h1>Serveurs</h1>
  </div>
  <ul class="mt-4">
    {% for server in servers %}
      <li>
        <a href="#">{{ server.name }}</a>
      </li>
    {% endfor %}
  </ul>
{% endblock %}
